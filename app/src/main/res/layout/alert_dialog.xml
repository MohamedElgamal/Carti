<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/alert_dialog_error_image"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@color/red"
        android:gravity="center"
        android:scaleType="fitCenter"
        android:src="@drawable/alert" />

    <TextView
        android:id="@+id/alert_dialog_error_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/alert_dialog_error_image"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="4dp"
        android:layout_marginTop="20dp"
        android:layout_marginRight="4dp"
        android:layout_marginBottom="20dp"
        android:gravity="center_horizontal"
        tools:text="Error Message Displayed Here may takes multiples lines"
        android:textColor="@color/red"
        android:textSize="18sp" />

    <Button
        android:id="@+id/alert_dialog_error_button"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_below="@id/alert_dialog_error_text"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:background="@drawable/flat_surface"
        android:gravity="center_vertical|center_horizontal"
        android:text="@string/ok"
        android:textColor="#ffffffff" />
</RelativeLayout>